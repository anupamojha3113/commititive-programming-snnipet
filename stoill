ll stoill(string z)
{
      ll ans=0;
      int count=0;
      for(int i=z.length()-1;i>=0;i--)
      {
            ans=ans+(z[i]-'0')*pow(10ll,count);
            count++;
      }
      return ans;
}

